# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!
# Alt key
set $mod Mod1

#Display config
#exec --no-startup-id /usr/bin/xrandr --output DP-1 --mode 3840x2160 --rate 60.00 --output eDP-1 --mode 1920x1080 --rate 59.93 --right-of DP-1
exec --no-startup-id /usr/bin/xrandr --dpi 135
#exec --no-startup-id /usr/bin/xrandr --output eDP-1 --scale 1.35x1.35
exec --no-startup-id /home/steven/.screenlayout/office-xrandr.sh

# Rename workspace
bindsym $mod+Shift+n exec /usr/bin/i3-input -F 'rename workspace to "%s"' -P 'New name: '

# Move workspace to different monitor
bindsym $mod+ctrl+Shift+Up move workspace to output up
bindsym $mod+ctrl+Shift+Down move workspace to output down 
bindsym $mod+ctrl+Shift+Left move workspace to output left
bindsym $mod+ctrl+Shift+Right move workspace to output right


#Start pulseaudio
exec --no-startup-id /usr/bin/start-pulseaudio-x11

#Load layout
#exec --no-startup-id "i3-msg 'workspace 1; append_layout /home/steven/Documents/i3/workspace-1.json'"
#Layout apps
#exec --no-startup-id /usr/bin/google-chrome
#exec --no-startup-id /usr/bin/onenote
#exec --no-startup-id /usr/bin/thunar
#exec --no-startup-id /usr/bin/gnome-terminal
#exec --no-startup-id /usr/bin/gnome-terminal

### Load bluetooth applet
exec --no-startup-id /usr/bin/blueman-applet

# Window key
#set $mod Mod4

#Autolock screen

exec xautolock -time 7 -locker 'i3lock -c000000'

#Network manager applet
exec --no-startup-id /usr/bin/nm-applet

#Clipboard
exec --no-startup-id /usr/bin/parcellite

#Fix annoying touch pad
# disable taps
#exec --no-startup-id /usr/bin/xinput set-prop 14 "Synaptics Tap Action" 0
exec --no-startup-id  /usr/bin/synclient TouchpadOff=3

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#
# Default font:
#font pango:monospace 8
#
# Better title font:
font xft:Roboto Sans 9 

## Various mouse controls ##
#
#
# The middle button and a modifer over any part of the window kills the window
bindsym --whole-window $mod+button2 kill
#
# The side buttons move the window around
#bindsym $mod+button9 move left
#bindsym $mod+button8 move right

# The right button toggles floating
# bindsym button3 floating toggle
#bindsym $mod+button3 floating toggle



## Various Compositors: 

#exec --no-startup-id /usr/bin/compton --vsync opengl -c -C -t-5 -l-5 -r4.2 -o.55 -f
exec --no-startup-id /usr/bin/compton --vsync opengl
#exec /usr/bin/xcompmgr -c -C -t-5 -l-5 -r4.2 -o.55 -f
#exec cairo-compmgr

# Volume Control
exec --no-startup-id /usr/local/bin/pasystray
exec --no-startup-id /usr/bin/xfce4-volumed
# Chat
exec --no-startup-id /usr/bin/slack

#Calendar
exec --no-startup-id /usr/bin/orage
bindsym Mod4+Shift+c [title="Orage"] scratchpad show
for_window [title="Orage"] move scratchpad

#Google Calendar
exec --no-startup-id google-chrome-hidpi --app='https://calendar.google.com'
for_window [class="calendar.google.com"] move scratchpad
bindsym Mod4+c [class="calendar.google.com" class="Google-chrome"] scratchpad show

#Slack
for_window [class="Slack"] move scratchpad
bindsym Mod4+s [title="Slack - Docker"] scratchpad show
bindsym Mod4+Shift+s [class="Slack"] focus

#nvPY
bindsym Mod4+Shift+n [class="nvPY" class="Nvpy"] focus

# Show terminal in scratchpad
bindsym Ctrl+space [class="gnome-terminal-server" class="Gnome-terminal"] scratchpad show

# Show Telegram (as long as it is running in xfce4-terminal)
exec --no-startup-id /usr/bin/xfce4-terminal -x telegram-cli
for_window [class="xfce4-terminal" class="Xfce4-terminal"] move scratchpad
bindsym Mod4+Shift+k [class="xfce4-terminal" class="Xfce4-terminal"] scratchpad show

### Window focus example
#Cases (salesforce)
##bindsym Mod4+s [class="Chromium" title="Console"] focus
#bindsym Mod4+s [class="Firefox" title="Console"] focus
#bindsym Mod4+Shift+s exec firefox --new-window "https://na8.salesforce.com/ui/support/servicedesk/ServiceDeskPage"

### Gmail / Inbox
bindsym Mod4+m [title="Inbox" class="Google-chrome"] focus
#for_window [class="calendar.google.com"] move scratchpad
bindsym Mod4+Ctrl+m [title="Inbox" class="Google-chrome"] scratchpad show

#Restore wallpaper
exec --no-startup-id /usr/bin/nitrogen

#Go For It todo list
for_window [class="go-for-it" class="Go-for-it"] move scratchpad
bindsym Mod4+l [class="go-for-it" class="Go-for-it"] scratchpad show 
#Power management
exec_always --no-startup-id xfce4-power-manager

## Disable Window Borders ##
# 
# Only disables the borders
#set $HIDE_BORDER border normal 0
#
# Toggle Enable/Disable Window Border AND Title
set $TOGGLE_BORDER border toggle
#set $SHOW_BORDER border normal 2

bindsym Mod4+Shift+b $TOGGLE_BORDER
#bindsym Mod4+Shift+n $SHOW_BORDER

#Basic Scratchpad (send to hidden workspace) configuration
#Make the currently focused window a scratchpad
bindsym $mod+Shift+minus move scratchpad
#
# # Show the first scratchpad window
bindsym $mod+minus scratchpad show
#

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# Window Titlebar colors

# class                 border  backgr. text    indicator
 client.focused          #4c7899 #2C82DD #ffffff #2e9ef4
 client.focused_inactive #333333 #222222 #ffffff #484e50
 client.unfocused        #333333 #4E5051 #DADADA #292d2e
 client.urgent           #2f343a #FF0707 #ffffff #900000

# start a terminal
#bindsym $mod+Return exec tilix
bindsym $mod+Return exec gnome-terminal

# kill focused window
bindsym $mod+Shift+q kill

# File manager
bindsym $mod+Shift+f exec thunar

# Screenlock
bindsym $mod+Ctrl+Shift+l exec /usr/bin/lockscreen

# i3lock
bindsym $mod+Ctrl+l exec i3lock -c000000


# Pulse Audio controls
### To determine sync number, use: pacmd list-sinks | grep -e 'name:' -e 'index'
### Active sink: pacmd list-sinks |awk '/* index:/{print $3}'
bindsym XF86AudioRaiseVolume exec --no-startup-id vol-up #increase sound volume
bindsym XF86AudioLowerVolume exec --no-startup-id vol-down #decrease sound volume
bindsym XF86AudioMute exec --no-startup-id vol-mute # mute sound

# Screenshot
bindsym --release Mod1+x exec --no-startup-id import /home/steven/Pictures/screenshot-$(date +%m%d-%H%M%S).png

##### Media Controls #####
#
### Your mileage may vary. 
#
#bindsym XF86AudioPlay exec playerctl play
#bindsym XF86AudioPause exec playerctl pause
#bindsym XF86AudioNext exec playerctl next
#bindsym XF86AudioPrev exec playerctl previous

# pause / play / next / previous
# bindsym XF86AudioPlay exec playerctl play-pause
# bindsym XF86AudioNext exec playerctl next
# bindsym XF86AudioPrev exec playerctl previous

bindsym XF86AudioNext exec sh /usr/bin/spotify-control next
bindsym XF86AudioPrev exec sh /usr/bin/spotify-control prev
bindsym XF86AudioPlay exec sh /usr/bin/spotify-control play
#bindsym Mod4+space exec sh /usr/bin/spotify-control play

# Show hidden spotify window (scratchpad)
bindsym Mod4+Shift+m [class="Spotify"] scratchpad show

#xcalc (calculator)
bindsym Mod4+Shift+x exec xcalc

# Show hidden Telegram (scratchpad)
# Note: You must use the set_title script to change the window title.
# I have tried to automate this.
bindsym Mod4+t [title="telegram-cli"] scratchpad show

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+semicolon move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus on child (after mod+a)
bindsym $mod+q focus child

# focus the child container
#bindsym $mod+d focus child

# switch to workspace
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9
bindsym $mod+Shift+0 move container to workspace number 10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 1 px or 1 ppt
        bindsym Down resize grow height 1 px or 1 ppt
        bindsym Up resize shrink height 1 px or 1 ppt
        bindsym Right resize grow width 1 px or 1 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Hide i3 bar toggle
bindsym $mod+m bar mode toggle bar-1
# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
#bar {
#        status_command i3status
#}


bar {
        status_command /usr/bin/conky-i3bar
        #tray_output DP-1-2
        # Note: "xrandr --output DP-1-2 --primary" may be necessary
        tray_output primary
        output DP-1-2
        font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
        font pango:Roboto Sans Mono 11
colors{
                background #000000
                statusline #ffffff
                separator #666666
# Class                             Border  Back.   Font
                focused_workspace  #45ADE5 #2C82DD #ffffff
#                 #                active_workspace   #45ADE5 #08344C #1883BC
#                 #                inactive_workspace #45ADE5 #000000 #1883BC
                urgent_workspace   #2f343a #FF0707 #ffffff
                }
}

bar {
        status_command /usr/bin/conky-i3bar
        #tray_output eDP-1-1
        output eDP-1-1
        font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
        font pango:Roboto Sans Mono 6
colors{
                background #000000
                statusline #ffffff
                separator #666666
# Class                             Border  Back.   Font
                focused_workspace  #45ADE5 #2C82DD #ffffff
#                 #                active_workspace   #45ADE5 #08344C #1883BC
#                 #                inactive_workspace #45ADE5 #000000 #1883BC
                urgent_workspace   #2f343a #FF0707 #ffffff
                }
}

bar {
        status_command /usr/bin/conky-i3bar
        #tray_output DP-1
        output HDMI-1-1
        font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
        font pango:Roboto Sans Mono 11
colors{
                background #000000
                statusline #ffffff
                separator #666666
# Class                             Border  Back.   Font
                focused_workspace  #45ADE5 #2C82DD #ffffff
#                 #                active_workspace   #45ADE5 #08344C #1883BC
#                 #                inactive_workspace #45ADE5 #000000 #1883BC
                urgent_workspace   #2f343a #FF0707 #ffffff
                }
}
